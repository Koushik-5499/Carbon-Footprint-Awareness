# The port the Express server will listen on
PORT=3000

# Secret key used for signing JWT tokens. 
# MUST be changed to a secure, random string in production!
JWT_SECRET=your_super_secret_jwt_key_here
